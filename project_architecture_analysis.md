# Social Analyzer 项目架构深度分析

## 🏗️ 项目整体架构

### 1. 架构模式: 模块化分层架构

```
┌─────────────────────────────────────────┐
│          CLI / Web API 接口层           │  ← app.js (Express服务器)
├─────────────────────────────────────────┤
│        扫描模式管理层                    │  ← 三种扫描模式
│  ┌──────────────┬──────────────┐        │
│  │ Fast Mode    │ Slow Mode    │        │
│  │ (HTTP)       │ (Selenium)   │        │
│  └──────────────┴──────────────┘        │
├─────────────────────────────────────────┤
│      核心检测引擎 (Detection Engine)     │  ← modules/engine.js
│  ┌──────────────────────────────────┐   │
│  │  Detection Logic                 │   │
│  │  - normal (字符串匹配)           │   │
│  │  - advanced (文本分析)           │   │
│  │  - ocr (图像识别)                │   │
│  └──────────────────────────────────┘   │
├─────────────────────────────────────────┤
│    辅助工具层 (Helper Layer)            │  ← modules/helper.js
│  - 配置管理                            │
│  - HTTP请求                            │
│  - 日志记录                            │
│  - 数据解析                            │
├─────────────────────────────────────────┤
│    数据层 (Data Layer)                  │  ← data/*.json
│  - sites.json (999个网站)              │
│  - names.json (名称数据库)             │
│  - dict.json (字典)                    │
│  - languages.json (语言)               │
└─────────────────────────────────────────┘
```

---

## 📋 核心模块详解

### 2.1 app.js - 入口和路由层

**职责:**
- Express HTTP服务器搭建
- CLI参数解析 (yargs)
- 路由分发 (/analyze_string, /get_logs, /save_settings等)
- 请求处理协调

**关键设计:**
- 使用 async/await 处理异步请求
- 基于UUID的会话管理
- 全局锁机制 (global_lock) 用于取消操作
- 日志文件按UUID隔离

**主要路由:**
- `POST /analyze_string` - 核心分析接口
- `GET /generate` - 用户名组合生成
- `POST /cancel` - 取消任务
- `GET /get_settings` - 获取配置
- `POST /save_settings` - 保存配置

---

### 2.2 三种扫描模式对比

| 特性 | Fast Mode (快速模式) | Slow Mode (慢速模式) | Special Mode (特殊模式) |
|------|---------------------|---------------------|------------------------|
| **技术栈** | HTTP请求 + Cheerio | Selenium WebDriver | 特殊规则引擎 |
| **并发数** | 15并发 (parallelLimit) | 8并发 (parallelLimit) | 自定义 |
| **检测类型** | normal only | normal + advanced + ocr | special rules |
| **资源消耗** | 低 | 高 (浏览器实例) | 中 |
| **准确率** | 中 | 高 | 极高 (特定网站) |
| **适用场景** | 快速扫描 | 深度分析 | Facebook/Gmail等特殊网站 |
| **重试机制** | 3层重试 | 无重试 | 自定义 |

**架构决策:**
- 三层重试机制仅用于Fast Mode (网络不稳定场景)
- Slow Mode使用Selenium绕过反爬 (JavaScript渲染)
- OCR集成在Slow Mode中处理验证码/图像文本

---

### 2.3 Detection Engine (检测引擎) - modules/engine.js

**核心设计模式: 策略模式 + 模板方法**

```javascript
// 检测流程模板
async detect(type, uuid, username, options, site, source, text_only, screen_shot)
  └─> detect_logic() // 实际检测逻辑
      ├─> normal detection (字符串匹配)
      ├─> advanced detection (文本分析)
      └─> ocr detection (图像识别)
```

**评分算法:**
```javascript
rate = (found / detections_count) * 100  // 0-100分
status = 
  rate >= 100 → 'good'      // 确定检测到
  rate >= 50  → 'maybe'     // 可能检测到
  else        → 'bad'       // 未检测到
```

**检测规则结构:**
```javascript
{
  type: 'normal' | 'advanced' | 'ocr' | 'shared',
  string: '匹配字符串',      // 支持 {username} 占位符
  return: 'true' | 'false',  // 期待结果
  name: '规则名称' (shared类型)
}
```

**Shared Detections (共享检测):**
- 减少重复规则
- 维护在 sites.json 顶层
- 通过 name 引用

---

### 2.4 Helper Module (辅助模块) - modules/helper.js

**设计模式: 工具类/服务定位器**

**核心功能分类:**

1. **配置管理**
   - 从JSON加载网站数据库
   - 全局配置 (detection_level, profile_template)
   - User-Agent、Proxy、Google API密钥

2. **HTTP请求**
   - `get_url_wrapper_text()` - 带重试机制的HTTP GET
   - 支持自定义header和proxy
   - 超时控制

3. **语言检测**
   - `get_language_by_parsing()` - HTML lang属性
   - `get_language_by_guessing()` - franc库检测

4. **日志系统**
   - 基于Promise的异步日志队列
   - 按UUID隔离日志文件
   - 控制台+文件双输出

5. **工具函数**
   - `compare_objects()` - 对象比较和排序
   - `get_site_from_url()` - URL解析
   - 字符串清理和验证

---

## 💾 数据架构

### 3.1 核心数据结构

**Website Entry (网站条目)**
```javascript
{
  url: "https://github.com/{username}",      // URL模板
  detections: [                               // 检测规则数组
    {type: "normal", string: "{username}", return: "true"}
  ],
  selected: "false" | "true",                // 是否选中
  timeout: 0,                                 // 超时(秒)
  implicit: 0,                                // 隐式等待(秒)
  extract: [],                                // 内容提取规则
  type: "Programming",                        // 网站类别
  global_rank: 89,                            // Alexa排名
  country: "United States",
  nsfw: "false" | "true"
}
```

**Profile Result (检测结果)**
```javascript
{
  username: "johndoe",
  link: "https://github.com/johndoe",
  rate: "%100.00",                           // 匹配度
  status: "good" | "maybe" | "bad",           // 检测状态
  title: "John Doe's GitHub",
  language: "en",
  country: "United States",
  rank: 89,
  text: "User profile content...",
  type: "Programming",
  metadata: [],                               // 元数据(可选)
  extracted: [],                              // 提取内容(可选)
  good: "true" | "false",                     // 是否通过阈值
  method: "find" | "get" | "all"              // 检测方法
}
```

### 3.2 配置文件结构

**data/sites.json** - 网站数据库
```json
{
  "project": {"name": "social-analyzer"},
  "shared_detections": [...],                // 共享检测规则
  "websites_entries": [...]                  // 网站列表(999个)
}
```

**data/names.json** - 名称来源数据库
```json
{
  "indian": { ... },
  "chinese": { ... },
  "russian": { ... },
  ... 50个国家和地区
}
```

**data/dict.json** - 名称字典
```json
{
  "m_names": [...],                         // 男性名字
  "f_names": [...],                         // 女性名字
  "prefix": [...]                           // 前缀
}
```

---

## 🎯 代码风格和架构原则

### 4.1 代码风格

**命名规范:**
- 函数名: `snake_case` (如 `find_username_normal`)
- 变量名: `snake_case` (如 `temp_profile`)
- 常量: `UPPER_SNAKE_CASE` (如 `detection_level`)
- 文件名: `kebab-case` (如 `fast-scan.js`)

**代码组织:**
- 每个模块单一职责
- 函数保持短小 (< 100行)
- 早期返回减少嵌套
- 错误处理集中化

**异步模式:**
- 统一使用 async/await
- Promise链明确
- 错误捕获使用 try-catch

### 4.2 架构原则

**SOLID原则应用:**

1. **S - 单一职责**
   - app.js: 只负责路由分发
   - engine.js: 只负责检测逻辑
   - helper.js: 只负责工具函数

2. **O - 开闭原则**
   - 新增检测类型无需修改引擎核心
   - 通过JSON配置添加新网站
   - 模块化设计支持扩展

3. **L - 里氏替换**
   - 三种扫描模式实现相同接口
   - 可以互相替换使用

4. **I - 接口隔离**
   - Fast Mode不需要Selenium
   - 按需导入模块 (动态导入)

5. **D - 依赖倒置**
   - 高层模块不依赖底层实现
   - 通过模块导入解耦

---

### 4.3 错误处理策略

**错误分类:**
1. **可恢复错误**: 网络超时、连接失败 → 重试机制
2. **不可恢复错误**: 无效URL、逻辑错误 → 早期返回
3. **外部错误**: Selenium崩溃、OCR失败 → 捕获并降级

**错误处理模式:**
```javascript
try {
  const result = await risky_operation()
  if (result === 'error') {
    // 可恢复错误
    return {return: 0, retry: true}
  }
  return {return: 1, data: result}
} catch (err) {
  // 不可恢复错误
  helper.verbose && console.log(err)
  return {return: 0, retry: false}
}
```

---

## 🔌 扩展点和设计模式

### 5.1 添加新社交媒体网站

**步骤:**

1. **编辑 data/sites.json**
```json
{
  "url": "https://newsite.com/user/{username}",
  "detections": [
    {
      "type": "normal",
      "string": "{username}",
      "return": "true"
    }
  ],
  "selected": "false",
  "timeout": 0,
  "implicit": 0,
  "extract": [],
  "type": "Social Networks",
  "global_rank": 999999,
  "country": "United States",
  "nsfw": "false"
}
```

2. **规则设计原则**
   - `type`: normal (字符串匹配最可靠)
   - `string`: 选择唯一的标识符
   - `return`: "true" (页面存在时匹配)
   - 测试URL确保不返回误报

3. **关键点**
   - 保持URL模板使用 `{username}` 占位符
   - 设置合理的 timeout (0 = 默认)
   - 选择准确的 type 分类
   - 如有需要添加 extract 规则

---

### 5.2 添加检测模式

**如果需要新的检测类型:**

1. **修改 engine.js**
```javascript
// 在 detect_logic() 函数中添加
else if (detection.type === 'new_type' && condition) {
  // 实现新检测逻辑
  temp_found = await new_detection_method()
}
```

2. **更新 detection_level 配置**
```javascript
// 在 helper.js 中
detection_level: {
  high: {
    fast: 'normal,new_type',  // 添加新类型
    ...
  }
}
```

---

### 5.3 添加新扫描模式

**如果需要第四种扫描模式:**

1. **创建 modules/new-scan.js**
```javascript
import helper from './helper.js'
import engine from './engine.js'

async function find_username_new(req) {
  // 实现新扫描逻辑
  // 保持相同接口: 返回 [profile1, profile2, ...]
}

export default { find_username_new }
```

2. **在 app.js 中导入并使用**
```javascript
import newScan from './modules/new-scan.js'

// 在路由中添加
if (req.body.option === 'FindUserProfilesNew') {
  profiles = await newScan.find_username_new(req)
}
```

---

### 5.4 修改评分算法

**当前算法:**
```javascript
rate = (found_detections / total_detections) * 100
```

**如果要加权:**
```javascript
// 在 engine.js 的 detect_logic() 中
const weights = {
  normal: 1,
  advanced: 2,    // 高级检测权重更高
  ocr: 3          // OCR最可靠
}

// 计算加权分数
let weighted_score = 0
let weighted_total = 0

detection_results.forEach(result => {
  const weight = weights[result.type] || 1
  weighted_score += result.found * weight
  weighted_total += weight
})

rate = (weighted_score / weighted_total) * 100
```

---

## 🎨 架构优缺点分析

### ✅ 优点

1. **模块化清晰**: 每个模块职责单一
2. **可扩展性强**: JSON配置驱动,无需改代码
3. **多种模式**: 适应不同场景需求
4. **容错性好**: 重试机制和降级处理
5. **并发控制**: async库限制并发,防止资源耗尽
6. **数据驱动**: 网站数据库易维护

### ⚠️ 缺点

1. **硬编码配置**: detection_level 在代码中,应移到配置文件
2. **全局状态**: helper.js 中有很多全局变量
3. **错误类型不明确**: 返回0/1,应使用Error对象
4. **测试覆盖不足**: 没有看到测试文件
5. **代码重复**: 三种扫描模式有重复逻辑

---

## 🎯 最佳实践和约定

### 6.1 添加新功能遵循的原则

✅ **应该做的:**
- 保持模块单一职责
- 使用 async/await 处理异步
- 添加详细日志 (log_to_file_queue)
- 处理边界情况 (空值、超时)
- 保持接口一致性
- 使用模板字符串 `{username}`

❌ **不应该做的:**
- 不要修改全局变量
- 不要删除现有检测规则
- 不要硬编码配置
- 不要使用回调地狱
- 不要忽略错误处理

### 6.2 JSON配置规范

**网站条目必须字段:**
- `url` (string, 必须包含 `{username}`)
- `detections` (array, 至少一个)
- `type` (string, 合理分类)
- `global_rank` (number, 0表示未知)

**检测规则规范:**
- `type` 必须是: normal, advanced, ocr, shared
- `string` 必须有实际匹配价值
- `return` 必须是: true 或 false

---

## 📖 总结

Social Analyzer 采用**模块化分层架构**,核心设计原则:

1. **关注点分离**: 路由、业务、数据层清晰
2. **策略模式**: 三种扫描模式可互换
3. **模板方法**: 检测引擎流程固定,具体实现可插拔
4. **数据驱动**: 网站和规则通过JSON配置
5. **防御性编程**: 详细的错误处理和日志

**扩展核心思路:**
- 修改JSON配置文件 (添加网站)
- 保持接口一致性 (输入输出格式)
- 遵循错误处理模式 (try-catch + 降级)
- 保持代码风格一致 (snake_case命名)
- 添加详细日志 (便于调试)

这个架构设计非常灵活,易于扩展,同时保持了代码的可维护性和可读性! 🎉
